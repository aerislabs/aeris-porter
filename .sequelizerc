var path = require('path');

var root = './app';

module.exports = {
  'config': path.resolve(root, 'config.json'),
  'migrations-path': path.resolve(root, 'migrations'),
  'models-path': path.resolve(root, 'models'),
  'seeders-path': path.resolve(root, 'seeders')
}
